// const initialConfigState = {
//   thing: {},
// };

// export default (state = initialConfigState, action = {}) => {
//   switch (action.type) {
//     case 'ALL_CONFIGS':
//       //  EXAMPLE
//       return Object.assign({}, state, { something: action.thing });
//     default: return state;
//   }
// };
